name: OpenWrt Builder

on:
  workflow_dispatch:
    inputs:
      device:
        description: 'Target device (all = build all devices)'
        required: false
        type: choice
        default: all
        options:
          - all
          - uz801
          - uf02
          - ufi103s

env:
  UPLOAD_FIRMWARE: true
  TZ: Europe/Madrid

jobs:
  resolve-version:
    runs-on: ubuntu-22.04
    outputs:
      openwrt_version: ${{ steps.resolve.outputs.version }}
    steps:
    - name: Resolve latest stable OpenWrt 25.12.x tag
      id: resolve
      run: |
        VERSION=$(git ls-remote --tags https://openwrt.org 'v25.12.*' | grep -oP 'v25\.12\.\d+$' | sort -V | tail -1)
        if [ -z "$VERSION" ]; then
          echo "ERROR: Could not resolve a stable OpenWrt version"
          exit 1
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT

  build:
    needs: resolve-version
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        device: ${{ fromJSON((inputs.device == 'all' || inputs.device == '') && '["uz801","uf02","ufi103s"]' || format('["{0}"]', inputs.device)) }}

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E ./devenv/dependencies.sh
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"

    - name: Clone OpenWrt source code
      run: |
        git clone https://openwrt.org openwrt
        git -C openwrt checkout ${{ needs.resolve-version.outputs.openwrt_version }}

    - name: Load msm89xx target and packages
      run: |
        chmod +x apply_patches.sh
        ./apply_patches.sh openwrt
        mv msm89xx openwrt/target/linux/msm89xx
        mv packages openwrt/package/msm8916
        
        # Меняем дефолтный IP на 15.1 во всей системе
        sed -i 's/192.168.1.1/192.168.15.1/g' openwrt/package/base-files/files/bin/config_generate

        # Если собираем ufi103s, подменяем DTS и настраиваем сеть
        if [ "${{ matrix.device }}" = "ufi103s" ]; then
          echo "Injecting custom DTS and network config for ufi103s..."
          
          # Подмена DTS (используем путь внутри msm89xx)
          DTS_DEST="openwrt/target/linux/msm89xx/files/arch/arm64/boot/dts/qcom/msm8916-uz801.dts"
          if [ -f "ufi103s.dts" ]; then
            mkdir -p $(dirname $DTS_DEST)
            cp ufi103s.dts $DTS_DEST
          fi

          # Настройка сети для USB
          mkdir -p openwrt/files/etc/config
          CONF="openwrt/files/etc/config/network"
          echo "config interface 'loopback'" > $CONF
          echo "    option device 'lo'" >> $CONF
          echo "    option ipaddr '127.0.0.1'" >> $CONF
          echo "    option proto 'static'" >> $CONF
          echo "" >> $CONF
          echo "config device" >> $CONF
          echo "    option name 'br-lan'" >> $CONF
          echo "    option type 'bridge'" >> $CONF
          echo "    list ports 'usb0'" >> $CONF
          echo "" >> $CONF
          echo "config interface 'lan'" >> $CONF
          echo "    option ipaddr '192.168.15.1'" >> $CONF
          echo "    option proto 'static'" >> $CONF
          echo "    option device 'br-lan'" >> $CONF
        fi

    - name: Update and install feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Load diffconfig
      run: |
        DEVICE="${{ matrix.device }}"
        DIFFCONFIG="diffconfig_${DEVICE}"
        [ -f "$DIFFCONFIG" ] && cp "$DIFFCONFIG" openwrt/.config

    - name: Download package
      run: |
        cd openwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec rm -f {} \; || true

    - name: Compile the firmware
      run: |
        cd openwrt
        make -j$(nproc) || make -j1 V=s
        echo "FILE_DATE=$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Organize files
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        mkdir -p firmware_bin
        find openwrt/bin/targets/ -type f \( -name "*.bin" -o -name "*.img" -o -name "*.manifest" -o -name "*.sh" \) -exec cp {} firmware_bin/ \;
        echo "FIRMWARE=$PWD/firmware_bin" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware
      uses: actions/upload-artifact@v4
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: openwrt_${{ matrix.device }}_${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}/*
